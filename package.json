{
  "name": "hit-server",
  "version": "1.0.0",
  "private": true,
  "description": "Ping sharewheels backend health endpoint on a loop",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js"
  }
}

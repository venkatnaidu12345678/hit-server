# hit-server

Keeps the ShareWheels backend awake by pinging its `/health` endpoint on a loop.

## Run locally

```bash
npm start
```

## Deploy on Render

Use as a **Background Worker** with start command `npm start`.
